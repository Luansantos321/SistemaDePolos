# core/urls.py
from django.urls import path

app_name = 'core'
urlpatterns = [
    # Aqui você pode colocar urls específicas do core, se quiser
    # Ex: path('dashboard/', views.dashboard, name='dashboard')
]
